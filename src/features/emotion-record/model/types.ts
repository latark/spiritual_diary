/** Эмоция, выбранная на колесе (структурно совпадает с SelectedEmotion из emotion-wheel). */
export interface RecordEmotion {
  familyId: string;
  familyName: string;
  shadeId: string;
  name: string;
  color: string;
}
